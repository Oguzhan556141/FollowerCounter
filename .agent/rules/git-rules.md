
# Git Usage Rules
- Asla `git` komutlarını (commit, push, pull, checkout vb.) kullanma.
- Versiyon kontrolü işlemlerini kullanıcı manuel olarak gerçekleştirecektir.
- Sadece dosya değişikliklerini ve kod geliştirmelerini yap, `git` ile ilgili herhangi bir eylemde bulunma.
